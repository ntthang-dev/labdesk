\* Same model with reservedForSomeoneElse() removed from handleLogin: proves
\* that check is what makes a booking actually reserve the machine.
SPECIFICATION Spec
CONSTANTS
    Students = {s1, s2}
    Machines = {m1, m2}
    Slots = {t1, t2}
    CurrentSlot = t1
    MaxBookings = 2
    UseLock = TRUE
    EnforceReservation = FALSE
INVARIANTS
    TypeOK
    NoDoubleBookedSlot
    OneMachinePerStudentPerSlot
    CapRespected
    NoWalkupStealsReservation
