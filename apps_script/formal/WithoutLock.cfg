\* Model of what Code.gs would be WITHOUT LockService: the race the lock
\* exists to prevent. Expected to find a NoDoubleBooking counterexample.
CONSTANTS
    Students = {s1, s2}
    Machines = {m1}
    UseLock = FALSE

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT NoDoubleBooking
INVARIANT OneMachinePerStudent
