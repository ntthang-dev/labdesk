\* Current Code.gs: book() inside LockService, reservedForSomeoneElse() wired
\* into handleLogin. Every safety property must hold.
SPECIFICATION Spec
CONSTANTS
    Students = {s1, s2}
    Machines = {m1, m2}
    Slots = {t1, t2}
    CurrentSlot = t1
    MaxBookings = 2
    UseLock = TRUE
    EnforceReservation = TRUE
INVARIANTS
    TypeOK
    NoDoubleBookedSlot
    OneMachinePerStudentPerSlot
    CapRespected
    NoWalkupStealsReservation
